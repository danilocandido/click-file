<% if file_system.file? %>
  <li><%= file_system.text %></li>
<% else %>
  <%= link_to file_system.text, show_modal_upload_path(id: file_system.id) , remote: true, data: { target: '#uploadpost', toggle: 'modal' }, class: 'nav-link'%>
<% end %>
